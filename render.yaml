services:
  - type: web
    name: painel-atendimento-rapido
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: EMAIL_USER
        value: vendashairuusstore@gmail.com
      - key: EMAIL_PASS
        sync: false
